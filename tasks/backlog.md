# Backlog

Waiting on customer feedback to finalize prioritization.

---

## Highest impact, lowest effort

- [ ] Error messaging — clear failure states when conversion fails or partially works

## Medium effort, high value for core audience

- [ ] Google Docs direct import — paste a URL or authorize via Google Drive API instead of exporting to .docx first
- [ ] Batch CLI output — convert an entire folder in one command (key for Confluence/wiki migrations)
- [ ] Markdown flavor options — GitHub Flavored Markdown vs standard vs Obsidian-compatible output

## Bigger swings

- [ ] Obsidian plugin — solves a real pain point for users migrating notes into Obsidian vaults; high distribution potential
- [ ] VS Code extension — right-click a file, convert to Markdown
- [ ] AI cleanup pass — optional post-conversion Claude pass to fix residual formatting, normalize headings, or summarize
